# Reports

Notebook-generated evaluation artifacts live here:

- `ragas_testset.json` / `ragas_testset.md`: frozen benchmark questions generated by RAGAS TestsetGenerator.
- `retrieval_evaluation.json` / `retrieval_evaluation.md`: retrieval comparison across BM25, vector, hybrid, Hybrid RRF, and Hybrid RRF + MMR, including RAGAS context metrics.
- `rag_answers.json`: generated or fallback answers with retrieved contexts for the frozen testset.
- `ragas_evaluation.json` / `ragas_evaluation.md`: RAGAS answer metrics from `ragas.evaluate()`; cached answer/metric usage is marked explicitly.
- `figures/`: notebook-ready comparison charts.

Generate and analyze these files from `notebooks/02_retrieval_experiments.ipynb` and `notebooks/03_ragas_evaluation.ipynb`. Deploy-facing Python code does not generate benchmark reports.

Run with `GEMINI_API_KEY` or `GEMINI_API_KEYS` configured to regenerate the RAGAS testset and answer metrics with Gemini. Keys must stay in `.env` or the shell environment and must not be committed.
